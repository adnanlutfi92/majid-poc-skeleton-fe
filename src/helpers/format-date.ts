export const basicFormat = (date: Date): string => {
  return date.toLocaleDateString()
}

export const dateOnlyFormat = (date: Date): string => {
  return date.toLocaleDateString()
}

export const timeOnlyFormat = (date: Date): string => {
  return date.toLocaleTimeString()
}
